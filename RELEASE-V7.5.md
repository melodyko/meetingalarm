# 会议铃 V7.5 发布说明

V7.5 修复邮件顶部“收件人姓名”可能显示乱码的问题，不改变会议数据和邮件正文。

## 本次更新

- 收件人由单独的邮箱字符串改为“姓名 + 邮箱地址”的标准邮件地址结构。
- Nodemailer 会将中文姓名自动编码为符合 RFC 规范的 UTF-8 邮件头。
- 测试邮件明确使用“管理员”作为收件人显示名，便于验证邮件头编码。
- 清理姓名中的回车和换行符，避免异常内容破坏邮件头。

## 注意

部分企业邮件客户端会使用内部通讯录覆盖邮件自带的显示名。如果升级后仍只在某一位收件人处乱码，需要同步检查企业邮箱通讯录中的员工姓名。
