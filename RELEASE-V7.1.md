# 会议铃 V7.1 发布说明

V7.1 是展示屏兼容性更新，后端数据结构、管理员密码和现有会议数据均不变，可直接覆盖 V7 程序文件升级。

## 本次更新

- 为 iPad mini 增加 721–900px 专用布局，优化横竖屏间距、按钮尺寸和周视图滑动体验。
- 手机端将会议列表调整为信息卡片，长会议名称、地点和发起人可自然换行。
- 手机端提醒弹窗使用全屏布局，保留手动关闭和自动消失逻辑，不影响声音播放。
- 主要触控按钮不小于 44px，适合平板长期展示和手机操作。
- 支持 iPhone/iPad 安全区域、旧版 Mobile Safari 的 Flex Gap 降级和 `-webkit-fill-available`。
- 保留每分钟局部数据刷新，不会重载页面或打断正在播放的提醒与弹窗。

## 升级方式

1. 停止当前 Node 服务。
2. 备份 `data/meeting-board.json` 和服务器环境变量。
3. 用 V7.1 包覆盖程序文件，不要覆盖生产数据文件。
4. 在项目目录执行 `npm ci` 和 `npm run build`。
5. 使用原有环境变量重新启动服务。

## 验证重点

- iPad mini 上分别测试横屏、竖屏、按日和按周查看。
- 手机上确认会议卡片无横向溢出，提醒弹窗可关闭。
- 首次打开页面后点击“启用声音”，再执行一次提醒测试。
