# 会议铃 V7 发布说明

发布日期：2026-07-30

## 已完成

- 人员名单支持 CSV 导入、导出。
- 导入采用同名合并策略，并由后端校验数量和邮箱格式。
- 测试邮件提供发送中、成功、失败 Toast 状态。
- 新增语音创建会议入口、浏览器录音、服务器转发和会议草稿回填。
- 语音 API Key 仅由服务器读取，不进入浏览器资源或接口响应。

## 语音接口接入状态

前端和服务器代理链路已经完成。正式启用前需要确认外部接口的：

1. 接口地址；
2. 鉴权 Header 格式；
3. 模型名称；
4. 请求字段和响应 JSON 格式。

当前适配器按 Bearer Token、Multipart 音频和 JSON 会议草稿设计。若外部接口格式不同，只需调整
`lib/meeting-voice-agent.ts`，会议表单和权限逻辑无需改动。

## 部署注意

- 覆盖生产源文件前，先备份 `data/meeting-board.json`。
- V7 不改变现有数据文件结构，可直接沿用 V6 数据。
- iPad 录音必须使用 HTTPS，并允许网站访问麦克风。
- 未配置 `VOICE_AGENT_ENDPOINT` 和 `VOICE_AGENT_API_KEY` 时，语音按钮会安全地保持禁用。
